[pytest]
markers =
    web: UI tests running against SauceDemo (Selenium)
    api: API tests running against JSONPlaceholder (requests)
    smoke: small, fast, high-value subset of tests
    regression: full regression coverage

log_cli = false
addopts = -ra --strict-markers
testpaths = web/tests api/tests
pythonpath = .
